# Changelog

> [!NOTE]
> Nothing has been released yet: there is no NuGet package, no GitHub release, and the tool is not installable.
> `0.1.0-preview.1` is the version the first package will carry; this section covers every change up to it and is regenerated when that package ships.
>
> Every section below is generated by Chartula itself and committed as written, not corrected by hand.
> Provider `anthropic`, model `claude-sonnet-5`, thinking `provider-default`, `--audience technical --no-publish`.
> What a run gets wrong here is tracked as an issue rather than edited away.

## 0.1.0-preview.1 - 2026-09-17

### Added

- Refuse the adaptive and disabled thinking-mode combinations that the Anthropic API rejects at configuration load, rather than failing only on the first request. ([#133](https://github.com/goldbarth/chartula/pull/133))
- Move the structure of every rendering out of the prompt and into code, so the model only writes the wording of each entry. ([#129](https://github.com/goldbarth/chartula/pull/129))
- Align the technical, customer and product prompts with their finished templates and rubrics, deciding template-level structure when the facts are built rather than in the prompt. ([#126](https://github.com/goldbarth/chartula/pull/126))
- Carry the labels of a change into the fact base and into changelog.json. ([#120](https://github.com/goldbarth/chartula/pull/120))
- Add an --audience option so a run renders only the named audiences instead of all three. ([#116](https://github.com/goldbarth/chartula/pull/116))
- Carry four missing customer format rules into the prompt: ordering actionable entries above others, ending on the last group rather than a closing line, collapsing non-actionable changes into one 'Also:' bullet, and requiring a checkable basis for any claim of degree. ([#112](https://github.com/goldbarth/chartula/pull/112))
- Warn at startup on stderr when a run has no GitHub token, explaining the lower rate limit and how to set one. ([#110](https://github.com/goldbarth/chartula/pull/110))
- Write the customer rendering as a publishable release-<tag>.md page with YAML front matter. ([#104](https://github.com/goldbarth/chartula/pull/104))
- Add a --no-publish flag that writes changelog.json and CHANGELOG.md while leaving the GitHub release notes unpublished. ([#101](https://github.com/goldbarth/chartula/pull/101))
- Add an openai-compatible LLM provider with a configurable base URL, reaching any endpoint that speaks the OpenAI chat-completions dialect. ([#88](https://github.com/goldbarth/chartula/pull/88))
- Add an llm.thinking configuration setting with provider-default, disabled and adaptive modes. ([#83](https://github.com/goldbarth/chartula/pull/83))
- Report each run's token usage and cost, broken down by rule-based check, thorough check and rephrasing. ([#66](https://github.com/goldbarth/chartula/pull/66))
- Add a categories configuration section for category order, display names and breaking-change prominence. ([#65](https://github.com/goldbarth/chartula/pull/65))
- Read chartula.yaml for configuration, with sensible defaults when no file is present. ([#64](https://github.com/goldbarth/chartula/pull/64))
- Add generate and preview CLI commands wired to the release pipeline. ([#63](https://github.com/goldbarth/chartula/pull/63))
- Store the technical, customer and product renderings in changelog.json. ([#62](https://github.com/goldbarth/chartula/pull/62))
- Write the generated technical text back to the release's GitHub release notes, updating rather than duplicating on a re-run. ([#61](https://github.com/goldbarth/chartula/pull/61))
- Write each release as a new section prepended to CHANGELOG.md, preserving existing history and replacing a re-run's section in place. ([#60](https://github.com/goldbarth/chartula/pull/60))
- Add an opt-in review mode that lets a maintainer approve or edit generated text before it is written. ([#59](https://github.com/goldbarth/chartula/pull/59))
- Add a thorough, LLM-based second-pass faithfulness check, on by default and toggled by a single setting. ([#58](https://github.com/goldbarth/chartula/pull/58))
- Add a rule-based faithfulness check that flags invented numbers, names and breaking-change claims at no token cost. ([#57](https://github.com/goldbarth/chartula/pull/57))
- Normalize formatting and enforce a single consistent voice across each audience's rendering. ([#56](https://github.com/goldbarth/chartula/pull/56))
- Render technical, customer and product audiences from one shared fact base, so they cannot contradict each other. ([#55](https://github.com/goldbarth/chartula/pull/55))
- Add a prompt architecture that rephrases only the established facts it is given, never inventing beyond them. ([#53](https://github.com/goldbarth/chartula/pull/53))
- Generate a changelog through the provider interface, making exactly one model call per release. ([#52](https://github.com/goldbarth/chartula/pull/52))
- Write the release fact base to changelog.json as a stable, versioned, machine-readable record. ([#51](https://github.com/goldbarth/chartula/pull/51))
- Make fact-base depth configurable between title only, title plus description, and title, description and linked issues. ([#50](https://github.com/goldbarth/chartula/pull/50))
- Assemble the release fact base from curated changes, resolving category, breaking flag, visibility and linked issues for each. ([#49](https://github.com/goldbarth/chartula/pull/49))
- Define ChangeFact as the per-change data model, capturing title, number, link, category, visibility, breaking flag and linked issues. ([#48](https://github.com/goldbarth/chartula/pull/48))
- Drop internal and chore changes from the changelog by default, while never dropping a breaking change. ([#47](https://github.com/goldbarth/chartula/pull/47))
- Add label rules that can exclude a change, force its category, or restrict the changelog to labelled changes only, driven from configuration. ([#46](https://github.com/goldbarth/chartula/pull/46))
- Assign each change a category deterministically from its Conventional Commits prefix before any model call. ([#45](https://github.com/goldbarth/chartula/pull/45))
- Fall back to commit data when a release has no merged pull requests or when a pull request's title is uninformative. ([#44](https://github.com/goldbarth/chartula/pull/44))
- Fetch the merged pull requests associated with a release's commits from the GitHub API. ([#43](https://github.com/goldbarth/chartula/pull/43))
- Read the commit range belonging to a release from git, falling back to full history when there is no previous tag. ([#42](https://github.com/goldbarth/chartula/pull/42))
- Abstract the LLM provider behind an IChangelogModel interface, with Anthropic as the first implementation. ([#41](https://github.com/goldbarth/chartula/pull/41))

### Fixed

- Fail a faithfulness check when the endpoint reports fewer input tokens than the prompt could have produced, instead of returning a clean-looking verdict on a truncated prompt. ([#130](https://github.com/goldbarth/chartula/pull/130))
- State in the customer prompt that an outcome must be drawn from the facts of the change, rather than only testing whether a written outcome qualifies. ([#123](https://github.com/goldbarth/chartula/pull/123))
- Derive a change's user-visibility from a visibility label when one is present, falling back to its category only when the labels are silent. ([#121](https://github.com/goldbarth/chartula/pull/121))
- Require a customer entry to state where a setting is configured only after its outcome, not in place of it. ([#118](https://github.com/goldbarth/chartula/pull/118))
- Raise the model's maximum output tokens so it has room to write after thinking, instead of leaving a rendering empty when thinking exhausts the ceiling. ([#114](https://github.com/goldbarth/chartula/pull/114))
- Keep a rendering's text as its body when splitting off a description would otherwise leave nothing behind. ([#109](https://github.com/goldbarth/chartula/pull/109))
- Require the outcome part of a customer entry rather than allowing it to be dropped, and remove the wording that let a model skip it. ([#106](https://github.com/goldbarth/chartula/pull/106))
- State the shape of a customer entry in the prompt - its groups, order, four-part structure, forbidden openings and setting placement - rather than leaving it to the model. ([#99](https://github.com/goldbarth/chartula/pull/99))
- Report a thorough faithfulness check that could not be parsed as unreadable, instead of indistinguishable from a check that found nothing. ([#80](https://github.com/goldbarth/chartula/pull/80))
- Remove the rule-based check for breaking-change claims, leaving breaking-change faithfulness to the thorough check. ([#74](https://github.com/goldbarth/chartula/pull/74))
- Distinguish a run in which an LLM call was never reported from one that was reported at a genuinely low cost. ([#73](https://github.com/goldbarth/chartula/pull/73))
- Fix truncated audience text caused by an unset output-token ceiling, and fix false breaking-change labels from matching the phrase anywhere in a commit body instead of only in its footer. ([#70](https://github.com/goldbarth/chartula/pull/70))
